//Business Logic
function Ticket(size-selection, gluten-selection, extraCheese-selection, meats-selection, nonMeats-selection) {
  this.size-selection =
  this.gluten-selection =
  this.extraCheese-selection =
  this.meats-selection =
  this.nonMeats-selection = 
}







window.addEventListener("load", function (){
  document.querySelector("form#movie-options").addEventListener("submit", handleFormSubmission);
});